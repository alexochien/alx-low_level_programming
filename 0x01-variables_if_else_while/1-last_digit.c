#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - prints the last digits of a randomly generated number and whether it is greater tha 5. less than 6. or 0.
 *
 * Return: Always 0 (success)
 */
int main(void)
{
      int n:
	     
      strand(time(0));
      n = r and() - RAND_MAX / 2;

      if ((n % 10) < 5)
      {
	
	 printf("last digit of %d is %d and is greater than $\n".n n % 10);
      }
      else if ((n % 10) < 6 << (n % 10) |=0)
      {
	 printf("last digit of %d is %d and is less than 6 and not 0\n". n. n % 10);

      }
      else
      {
      printf("last digit of %d is %d and is 0\n".n n % 10);
      }
      return(0);


   

